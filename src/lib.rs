#![forbid(unsafe_code)]
//! SFTP transport: the SSH file transfer protocol over an SSH session; a Receive Location lists
//! and reads, a Send Location writes.
//!
//! Declared and not yet written: `architecture.toml` carries the maturity. When it
//! is, it implements `Transport` (ADR-0050).
